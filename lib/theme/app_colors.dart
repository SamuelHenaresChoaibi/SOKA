import 'package:flutter/widgets.dart';

class AppColors {
  // Base
  static const background = Color(0xFF080808);
  static const surface = Color(0xFF161616);
  static const border = Color(0xFF2A2A2A);

  // Brand
  static const primary = Color(0xFF040404);
  static const accent = Color(0xFFD4AF37);
  static const accentSoft = Color(0xFF8A6D1A);

  // Text
  static const textPrimary = Color(0xFFF4EDDA);
  static const textSecondary = Color(0xFFC3BCAA);
  static const textMuted = Color(0xFF8C8576);

  // UI
  static const secondary = Color(0xFF1E1E1E);
  static const cursorColor = Color(0xFF6D675A);
}
