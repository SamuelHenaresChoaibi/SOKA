import 'package:flutter/widgets.dart';

class AppColors {
  // Base neutrals (escala profesional)
  static const background = Color(0xFFF6F7F9); // fondo claro limpio
  static const surface = Color(0xFFFFFFFF); // cards
  static const border = Color(0xFFE6E8EC);

  // Brand
  static const primary = Color(0xFF121212); // negro elegante
  static const accent = Color(0xFFE7C91F);

  // Text
  static const textPrimary = Color(0xFF1C1C1E);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);

  // UI
  static const secondary = Color(0xFFF0F2F5);
  static const cursorColor = Color(0xFF9AA0A6);
}
